# ICD 工具原型 · 服务器部署说明

本文档面向**服务器运维人员**。部署完成后，使用方只需在浏览器打开一个网址即可使用该工具，**客户端无需安装任何软件**。

---

## 0. 最快路径

把交付包拷到服务器，进入该目录执行一条命令：

```bash
bash deploy.sh
```

脚本依次完成：**校验归档 → 导入镜像 → 复核 CPU 架构与目录 → 启动并健康自检**，最后打印访问地址。首次部署前记得填 `backend/.env`（见 §5.3）。

> 只有需要看每一步到底做了什么、或脚本因环境受限无法执行时，才需要看 §6 的手工部署。

---

## 1. 部署形态

一个 Docker 容器、一个端口：容器内的后端服务既提供分析接口，也提供前端页面（同源访问）。

```
使用方浏览器  ──HTTP/8000──▶  服务器: Docker 容器（icd-tool-backend-v4.0）
                                    ├── /            前端页面（容器内 /app/static）
                                    ├── /api/v4/*    分析接口
                                    └── /output      分析结果（落宿主机目录）
```

---

## 2. 环境要求

| 项 | 要求 |
| --- | --- |
| 操作系统 | Linux（x86_64 或 aarch64 均可，但**必须与交付镜像的架构一致**，见 §5.1） |
| CPU 架构 | 与镜像一致。用 `uname -m` 确认；与镜像不符时容器无法启动（`exec format error`） |
| 容器运行时 | Docker ≥ 24.0，且含 `docker compose` 插件，Compose ≥ v2.26 |
| 端口 | **8000/tcp**，需对使用方网段开放 |
| 磁盘 | 建议 ≥ 20 GB（分析输出会持续累积，见 §8.4） |
| 内存 | 空闲约 270 MB；实际分析时占用明显上升，建议 ≥ 4 核 / 8 GB |
| 网络 | 需能访问**内网模型服务地址**（在 `.env` 中配置）；**不需要访问公网** |
| 其他 | 需有 `bash` 与 `sha256sum`；`curl` 或 `wget` 用于启动后的自检（没有则跳过自检） |

> Compose 版本要求说明：编排文件使用了 `env_file` 的 `path` / `required` 长语法（Compose ≥ v2.24）与 `pull_policy`（Compose ≥ v2.20）。若版本过低，`deploy.sh` 会自动去掉 `pull_policy` 一行重试（见 §9）。

---

## 3. 交付物清单

| 文件 | 说明 |
| --- | --- |
| `deploy.sh` | **一键部署脚本**，见 §5 |
| `BUILD_INFO` | 构建信息：镜像标签、架构、归档 sha256、源码版本。脚本据此校验，请勿改动 |
| `icd-tool-backend.tar.gz` | 后端镜像归档，用 `docker load` 导入 |
| `icd-tool-backend.tar.gz.sha256` | 校验值 |
| `docker-compose.server.yml` | 容器编排文件 |
| `部署说明.md` | 本文件 |
| `frontend/dist/` | 前端页面产物（**无需 Node.js**，原样放置即可） |
| `backend/.env.example` | 配置模板；正式 `.env`（含密钥）**经单独渠道提供**，不在交付包内 |
| `backend/output/` | 输出目录，空目录即可 |

---

## 4. 目录结构

以下相对位置**必须保持一致**（`docker-compose.server.yml` 中的路径按此解析，`deploy.sh` 也要求自己位于本目录）：

```
部署目录/
├── deploy.sh
├── BUILD_INFO
├── docker-compose.server.yml
├── 部署说明.md
├── icd-tool-backend.tar.gz
├── icd-tool-backend.tar.gz.sha256
├── frontend/
│   └── dist/                 # 前端页面产物
└── backend/
    ├── .env                  # 模型服务配置（deploy.sh 在缺失时从 .env.example 复制）
    ├── .env.example
    └── output/               # 输出目录（可为空）
```

---

## 5. 一键部署（deploy.sh）

### 5.1 它做的四件事

```bash
bash deploy.sh                  # 完整流程
bash deploy.sh --no-start       # 只导入镜像与准备目录，不启动容器
bash deploy.sh --skip-verify    # 跳过 sha256 校验（仅在无法校验时使用）
```

1. **校验归档**：比对 `icd-tool-backend.tar.gz` 的 sha256 与 `BUILD_INFO` 记录，不一致直接中止（避免拿了半截文件部署）。
2. **复核架构**：把 `uname -m` 与 `BUILD_INFO` 里的 `arch=` 对照，不一致时给出明确提示——而不是等到容器启动时报 `exec format error`。
3. **导入镜像并准备目录**：`docker load`，创建 `backend/output/`、`frontend/dist/`，缺失 `backend/.env` 时从 `.env.example` 复制一份。
4. **启动并自检**：`docker compose up -d`，然后轮询 `/api/v4/health`（最多 60 秒），再确认前端首页返回 200，最后打印访问地址。

脚本是**幂等**的：重复执行只会重新导入镜像并按新镜像重建容器，**不会**动 `backend/output/` 里的历史结果，也不会覆盖已有的 `backend/.env`。

### 5.2 执行

```bash
cd <部署目录>
bash deploy.sh
```

期望输出结尾：

```
部署完成
  访问地址：http://<服务器IP>:8000
```

### 5.3 首次部署：配置模型服务

`deploy.sh` 若发现 `backend/.env` 不存在，会从 `.env.example` 复制一份模板——**模板里的地址与密钥还是空的，必须填好才能跑真实分析**（只跑自检可以先保持 `USE_MOCK_LLM=1`）。

| 字段 | 说明 |
| --- | --- |
| `USE_MOCK_LLM` | `0` = 调用真实模型；`1` = 模拟模式（不调模型，用于连通性自检） |
| `JUDGE_PROVIDERS` | 参与裁判的模型列表，逗号分隔，取值 `deepseek,minimax,qwen`；**列出的每个模型都必须有对应配置** |
| `DEEPSEEK_BASE_URL` / `DEEPSEEK_MODEL` / `DEEPSEEK_API_KEY` | 第一个模型服务地址、模型名、密钥 |
| `MINIMAX_BASE_URL` / `MINIMAX_MODEL` / `MINIMAX_API_KEY` | 第二个模型服务（不使用时须从 `JUDGE_PROVIDERS` 中移除） |
| `QWEN_BASE_URL` / `QWEN_MODEL` / `QWEN_API_KEY` | 第三个模型服务（同上） |

注意：

- 模型服务需为 **OpenAI 兼容**接口（`/v1/chat/completions`）。`*_BASE_URL` 写 `http://IP:端口` 或 `http://IP:端口/v1` 均可。
- 若内网模型服务**不校验鉴权**，`*_API_KEY` 仍需填写任意非空值。注意：密钥是在**真正调用该模型时**才检查的，不是启动时——留空的后果是「服务能正常起来，跑分析时才报 `XXX_API_KEY not set`」。
- 完整字段说明（含超时、降级参数）见交付包内的 `backend/.env.example`。
- 改完 `.env` **必须重建容器**才生效（`restart` 不会重新读 `.env`）：

```bash
docker compose -f docker-compose.server.yml up -d --force-recreate backend
```

### 5.4 验收

```bash
# 1) 后端健康检查
curl -s http://127.0.0.1:8000/api/v4/health
# 期望: {"status":"ok","api_version":"v4"}

# 2) 前端页面
curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8000/
# 期望: 200

# 3) 容器状态
docker compose -f docker-compose.server.yml ps      # 状态应为 running
```

最后在**使用方电脑的浏览器**打开 `http://<服务器IP>:8000`，应看到文件上传页面。

### 5.5 首次联调建议（真实模型）

先用 `USE_MOCK_LLM=1` 跑通一次完整分析（上传一组样例文件 → 生成报告），确认**页面、接口、输出落盘**三者正常；再改为 `USE_MOCK_LLM=0` 并用真实模型跑一次，确认模型链路可用。

---

## 6. 手工部署（等价步骤）

脚本不可用时按以下步骤操作，与 `deploy.sh` 完全等价。

```bash
cd <部署目录>

# 1) 校验交付物（可选但建议）
sha256sum -c icd-tool-backend.tar.gz.sha256

# 2) 确认架构与 BUILD_INFO 中的 arch= 一致
uname -m          # x86_64 → linux/amd64，aarch64 → linux/arm64

# 3) 导入镜像
gunzip -c icd-tool-backend.tar.gz | docker load
docker images | grep icd-tool-backend      # 应看到 icd-tool-backend-v4.0

# 4) 准备目录与配置
mkdir -p backend/output
cp backend/.env.example backend/.env       # 首次部署；随后编辑填入模型地址与密钥

# 5) 启动
docker compose -f docker-compose.server.yml up -d
```

> 归档为**双格式**（同时含经典 `manifest.json` 与新式 `oci-layout`），新旧 Docker 均可读取。若仍报镜像格式（manifest / media type）相关错误，请将完整报错回传，我们会按目标机版本重新导出。

---

## 7. 日常运维

### 7.1 日志

```bash
docker compose -f docker-compose.server.yml logs -f backend
```

### 7.2 停止 / 启动 / 重启

```bash
docker compose -f docker-compose.server.yml down       # 停止并删除容器（镜像保留）
docker compose -f docker-compose.server.yml up -d      # 启动
docker compose -f docker-compose.server.yml restart    # 重启
```

### 7.3 前端页面单独更新（不用换镜像）

`frontend/dist/` 是**只读挂载**的宿主机目录，只改前端时无需重建镜像：

```bash
# 用新的 dist 覆盖 ./frontend/dist 后刷新浏览器即可（必要时清一次浏览器缓存）
```

改后端（Python 代码、依赖）则必须换镜像，见 §8.3。

### 7.4 输出文件与磁盘

分析结果写入 `backend/output/v4/<任务ID>/output/`，包含 Word 报告、Excel 清单及中间文件。

```bash
du -sh backend/output                    # 查看占用
ls -t backend/output/v4 | tail           # 查看最近任务
```

输出文件由**使用方**判断是否保留，建议约定定期清理策略。单次分析产生的中间文件可达数百 MB 量级，请留意磁盘水位。

---

## 8. 更新版本

### 8.1 用新交付包更新

```bash
# 在旧部署目录旁解开新交付包，或直接用新目录 + 把 backend/output 指过去
bash deploy.sh
```

`deploy.sh` 会导入新镜像并重建容器。若沿用旧目录，请把 `backend/output/` 与 `backend/.env` 保留/拷回，历史结果与配置都不丢。

> **注意**：容器重建会中断**正在进行中**的分析任务（页面会提示「继续 / 放弃」）。请在无任务运行时更新。

### 8.2 判断交付包新旧

看 `BUILD_INFO` 的构建时间与源码版本：

```bash
cat BUILD_INFO
```

### 8.3 哪些改动需要换镜像

| 改动内容 | 是否要换镜像 |
| --- | --- |
| 前端页面（`frontend/dist`） | 不需要，覆盖目录即可（§7.3） |
| 后端 Python 代码、依赖、模型调用逻辑 | **需要**（新镜像 + `deploy.sh`） |
| 编排文件、`.env` | 不需要（重新 `up -d` / `--force-recreate`） |

---

## 9. 常见问题

| 现象 | 排查方向 |
| --- | --- |
| `deploy.sh` 报「归档校验失败」 | 文件在拷贝中损坏或不完整，重新拷贝一次 |
| `deploy.sh` 报「镜像架构与本机不一致」 | 交付包架构与服务器不符，需要对应架构的镜像（§5.1） |
| 提示不认 `pull_policy` | Compose 版本偏低。`deploy.sh` 会去掉该行重试并生成 `docker-compose.server.effective.yml`；手工部署时删掉该行即可 |
| 浏览器打开是空白页或 404 | 确认 `frontend/dist/` 存在且已挂载：`docker inspect <容器名> --format '{{json .Mounts}}'` |
| 页面能开，但接口报错 | 看容器日志；确认 `backend/.env` 中 `BASE_URL` / `API_KEY` / `JUDGE_PROVIDERS` 三者一致 |
| 模型调用超时 | 容器到模型服务地址的网络连通性（`docker exec <容器名> curl -s <BASE_URL>/v1/models`） |
| 分析结果为空或报模型错误 | 确认模型服务为 OpenAI 兼容接口；必要时先用 `USE_MOCK_LLM=1` 自检链路 |
| 容器起不来，日志报 `exec format error` | 镜像架构与服务器不符，需更换对应架构的镜像 |
| 上传 WPS 另存出的 `.docx` / `.xlsx` 报解析失败或 500 | **本版本已修复**（WPS 会把包内条目名写成反斜杠分隔，旧版本在 Linux 上解析失败）。若仍复现，请确认镜像已更新到本交付包 |
| 任务列表里出现「等待开始」且没有进度、没有日志 | **本版本已修复**：上传失败（文件过大、追溯表格式错、识别不出系统类型）不再留下这类空记录；历史遗留的记录现在可以直接点「放弃」去掉 |
| 页面提示任务已中断 / 已终止 | 容器重启或任务被终止。点「继续」按原参数续跑（已上传文件无需重传），或点「放弃」结束该任务 |
| 上传 `.doc`（旧版 OLE 格式） | 不支持，请先另存为 `.docx`；EoICD 表格请另存为 `.xlsx` |
| 外网无法访问 8000 | 防火墙 / 网段策略 |

---

## 10. 注意事项

1. **无登录鉴权**：任何能访问 8000 端口的人都可以使用该工具并消耗模型服务额度，建议通过网络策略限制来源网段。
2. **任务状态保存在内存中**：容器重启或重建会丢失正在进行的任务，其余历史任务不受影响（页面会列出被中断的任务供「继续 / 放弃」）。
3. **输出目录为宿主机目录**：容器重建不丢数据；但删除 `backend/output/` 会删除历史分析结果。
4. **不写系统目录、不注册系统服务**：所有改动限于容器与部署目录，卸载即清理。

---

## 11. 卸载

```bash
docker compose -f docker-compose.server.yml down
docker rmi icd-tool-backend-v4.0
```

---

## 12. 附：交付包是怎么构建出来的（构建方看，运维可跳过）

交付包由仓库里的 `scripts/build-image.sh` 生成（**该脚本属构建方工具，不随交付包提供**）：

```bash
bash scripts/build-image.sh        # 产出 dist/icd-deploy-<日期>/ 与同名 .tar
```

它做四件事：`npm run build` 生成前端产物 → `docker build` 构建后端镜像 → `docker save` 导出归档并算 sha256 → 组装目录（含本文档与 `deploy.sh`）并写 `BUILD_INFO`。

**为什么不能只把镜像给别人**：服务器形态是「一个后端容器同时提供接口与页面」，前端**不打成镜像**，而是把 `frontend/dist` 挂进后端容器。所以一次可交付的构建必须同时包含**后端镜像归档**与**前端构建产物**两件东西，外加编排文件、部署脚本与说明；只用 `docker compose build` 拿到的是开发用的镜像，不含前端产物，服务器上会打开是空白页。把这一串固定成脚本，也避免再出现「重建了前端镜像，但服务器用的 `dist` 没更新」这类错配。
