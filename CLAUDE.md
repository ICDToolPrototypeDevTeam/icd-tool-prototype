# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a prototype ICD tool repository. The `.gitignore` indicates a Python project (`.venv/`, `__pycache__/`, `.pyc` patterns).

## Commands

Commands will be added once the project structure is established.

## Architecture

Architecture documentation will be added once code is committed.